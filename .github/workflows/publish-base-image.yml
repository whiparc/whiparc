name: Publish DevOps Base Images to GHCR

on:
  push:
    branches:
      - main
    paths:
      - 'apps/api/Dockerfile.base'
      - 'sandbox/Dockerfile.ssh.base'
      - '.github/workflows/publish-base-image.yml'
  workflow_dispatch:

jobs:
  publish:
    name: Build & Publish Base Images
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Log in to GitHub Container Registry (GHCR)
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and Push Runner Base Image
        uses: docker/build-push-action@v7
        with:
          context: ./apps/api
          file: ./apps/api/Dockerfile.base
          push: true
          tags: ghcr.io/${{ github.repository_owner }}/whiparc-runner-base:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build and Push Sandbox Base Image
        uses: docker/build-push-action@v7
        with:
          context: ./sandbox
          file: ./sandbox/Dockerfile.ssh.base
          push: true
          tags: ghcr.io/${{ github.repository_owner }}/whiparc-sandbox-base:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max
